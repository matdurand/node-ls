{
  "name": "node-ls",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1",
    "prettier": "^2.0.5",
    "monorepo-run": "^0.4.2"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "node --max-old-space-size=1900 ./node_modules/.bin/lerna run build",
    "start": "mono-run start --stream 500"
  }
}